{
  "$schema": "http://json-schema.org/draft/2019-09/schema#",
  "title": "Generic schema for the data passed to a grading cloud function.",
  "type": "object",
  "properties": {
    "answer": { "type": "string" },
    "response": { "type": "string" },
    "params": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  "required": ["answer", "response"],
  "additionalProperties": false
}
